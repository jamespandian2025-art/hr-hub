const displayFont = "var(--font-body)"
const bodyFont = "var(--font-body)"

export default function LocationsPage() {
  return (
    <div style={{ fontFamily: bodyFont }}>
      <div style={{ marginBottom: 28 }}>
        <h1 style={{ fontFamily: displayFont, fontSize: 26, fontWeight: 700, color: '#111827', letterSpacing: '-0.02em', marginBottom: 6 }}>
          Locations
        </h1>
        <p style={{ fontSize: 14, color: '#535353' }}>This section is coming soon.</p>
      </div>
    </div>
  )
}
